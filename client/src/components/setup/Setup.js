import React, { Fragment } from "react";
import WelcomeSummary from "./WelcomeSummary";

const Setup = (props) => {
  return (
      <Fragment>
          <WelcomeSummary />
      </Fragment>
  );
};
export default Setup;
